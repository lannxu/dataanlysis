services:
  talent-review:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:3001:3001"
    environment:
      PORT: ${PORT:-3001}
      LISTEN_HOST: ${LISTEN_HOST:-0.0.0.0}
      PUBLIC_URL: ${PUBLIC_URL:-}
    volumes:
      - ./data:/app/data